/**
 * Built-in rules registry
 *
 * Add your rules here as you create them
 */

import type { Rule } from "@/rules/types.js";

/**
 * All built-in rules
 *
 * Start by adding your first rule here!
 * Example:
 * import { noLargeFunctionsRule } from './no-large-functions.js';
 */
export const builtInRules: Rule[] = [
  // Add rules here as you create them
  // They will be automatically registered when the analyzer runs
];
