export function scheduleWork(values: number[]): void {
  if (values.length > 0) {
    values.forEach(() => {
      values.map(() => {
        if (values.length > 1) {
          while (values.length > 2) {
            return values.length;
          }
        }
        return values.length;
      });
    });
  }
}