const flag = true;
const loading = true;
const isNotDisabled = false;
const usersArray: string[] = [];

function user(): boolean {
  return true;
}

function process(data: string) {
  const result = data.trim();
  return result;
}

const values = ["a"];
values.map((item) => item.length);
const result = await Promise.all([Promise.resolve(1)]);
const data = { data: "ok" }.data;

const click = () => {};
const handleChange = () => {};

void [flag, loading, isNotDisabled, usersArray, user, process, result, data, click, handleChange];