const permitted = true;
const usr = { id: 1 };
const cfg = { active: true };

function update(payload: string) {
  return payload;
}

const onSubmit = () => {};

void [permitted, usr, cfg, update, onSubmit];